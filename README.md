# 🍽️ Restaurant Management System (RMS)

This is a **console-based application** built with Python and SQLAlchemy designed to manage restaurant operations, including tables, menu items, orders, and sales reporting using a PostgreSQL database.

## ✨ Features

* ✅ **Menu Management:** Adding new items and updating prices.
* 🪑 **Table Control:** Adding, removing, and viewing table availability.
* 🧾 **Order Processing:** Creating new orders and managing order details.
* ⏳ **Status Updates:** Tracking order status (Pending, Done, Canceled).
* 💰 **Sales Reporting:** Generating daily sales summaries.

## 🛠️ Prerequisites

To run this project, ensure you have the following installed:

* 🐍 **Python 3.8+**
* 🐘 **PostgreSQL** server running locally or accessible via network.

## 🚀 Installation and Setup

### 1. Install Dependencies

First, navigate to the project directory and install the required Python packages using `requirements.txt`:

```bash
pip install -r requirements.txt